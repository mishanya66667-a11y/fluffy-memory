# 🤖 AI Call Center - Полностью Автоматизированный (Linux Edition)

**Asterisk → AGI → AI** прямая связка без веб-сервера!

Звони и общайся с AI через обычный телефон или софтфон. **Полная автоматическая установка** всех зависимостей на Linux.

## ✨ Главные фишки

- ✅ **Полная автоустановка** - Docker, Ollama, зависимости устанавливаются автоматически
- ✅ **Два режима** - интерактивный (с вопросами) и автоматический (без UI)
- ✅ **Один скрипт** - всё в одном: `sudo ./install.sh`
- ✅ **Нулевая настройка** - работает из коробки
- ✅ **Безопасность** - случайные пароли, изоляция Docker
- ✅ **Множество AI** - Claude, GPT, Gemini, Ollama
- ✅ **Голос** - Google/Yandex/OpenAI TTS/STT

---

## 🚀 Быстрый старт

### Вариант 1: Интерактивная установка (рекомендуется для первого раза)

```bash
# Клонируем репозиторий
git clone https://github.com/YOUR_REPO/ai-callcenter-direct.git
cd ai-callcenter-direct

# Запускаем установку с правами root
sudo chmod +x install.sh
sudo ./install.sh
```

Скрипт **автоматически**:
1. Установит Docker и Docker Compose
2. Установит Ollama и загрузит модель llama2
3. Установит Python зависимости
4. Запустит интерактивную настройку проекта
5. Соберёт и запустит Docker контейнер

**Готово!** Через 5-10 минут у вас будет работающий AI колл-центр.

---

### Вариант 2: Полностью автоматическая установка (для CI/CD, серверов)

Для автоматического деплоя без вопросов:

```bash
sudo chmod +x install_auto.sh
sudo ./install_auto.sh
```

Или с кастомными настройками:

```bash
sudo SERVER_IP=10.0.0.5 \
     AI_PROVIDER=anthropic \
     AI_MODEL=claude-sonnet-4-5-20250929 \
     ANTHROPIC_API_KEY=your_key_here \
     ./install_auto.sh
```

#### Все переменные окружения:

```bash
# Базовые
PROJECT_NAME=ai-callcenter      # Название проекта
SERVER_IP=192.168.1.100         # IP сервера (авто-определяется)
SIP_PORT=5060                   # SIP порт
RTP_START=10000                 # RTP начальный порт
RTP_END=10100                   # RTP конечный порт

# SIP пользователи
SIP_USER=user1                  # Логин
SIP_PASSWORD=auto_generated     # Пароль (генерируется)
SIP_EXTENSION=100               # Внутренний номер
SIP_DISPLAY_NAME=User1          # Имя для отображения

# AI настройки
AI_PROVIDER=ollama              # anthropic | openai | google | ollama
AI_MODEL=llama2                 # Модель AI
OLLAMA_URL=http://localhost:11434
AI_LANGUAGE=русский             # Язык общения
AI_SYSTEM_PROMPT="Ваш промпт"   # Системный промпт

# API ключи (если нужно)
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...
GOOGLE_API_KEY=...

# Голосовые настройки
TTS_PROVIDER=openai             # google | yandex | openai
STT_PROVIDER=whisper            # google | yandex | whisper
ENABLE_RECORDING=false          # Запись разговоров
```

---

### Вариант 3: Однострочная установка (совсем для ленивых 😎)

```bash
curl -fsSL https://raw.githubusercontent.com/YOUR_REPO/ai-callcenter-direct/main/install.sh | sudo bash
```

⚠️ Проверяйте скрипты перед запуском с `sudo`!

---

## 📁 Что устанавливается автоматически

| Компонент | Описание | Версия |
|-----------|----------|--------|
| **Docker Engine** | Контейнеризация | Latest stable |
| **Docker Compose** | Оркестрация | V2 (plugin) |
| **Ollama** | Локальные LLM модели | Latest |
| **llama2** | AI модель (3.8 GB) | Latest |
| **Python 3** | Runtime для AGI скриптов | System |
| **Asterisk** | VoIP сервер | 20.x (в Docker) |

---

## 🎯 Как это работает

```
┌─────────────┐
│  SIP Client │ (ваш софтфон: MicroSIP, Zoiper, Linphone)
└──────┬──────┘
       │ SIP вызов на 192.168.1.X:5060
       ↓
┌─────────────────────────────────────────────────┐
│  Docker Container: ai-callcenter                │
│                                                 │
│  ┌────────────┐                                 │
│  │  Asterisk  │ принимает SIP вызов             │
│  └──────┬─────┘                                 │
│         │ AGI протокол (stdin/stdout)           │
│         ↓                                       │
│  ┌────────────────┐                             │
│  │ agi_handler.py │ Python AGI скрипт           │
│  ├────────────────┤                             │
│  │ 1. Приветствие │ → TTS синтез                │
│  │ 2. Слушает     │ → STT распознавание         │
│  │ 3. AI обработка│ → Ollama/Claude/GPT         │
│  │ 4. Ответ       │ → TTS синтез                │
│  │ 5. Повтор      │ → Цикл                      │
│  └────────────────┘                             │
│         ↓                                       │
│  ┌────────────┐                                 │
│  │   Ollama   │ на хосте (localhost:11434)      │
│  │   llama2   │                                 │
│  └────────────┘                                 │
└─────────────────────────────────────────────────┘
```

---

## 🔧 Поддерживаемые AI провайдеры

### 1. Ollama (по умолчанию) 🆓

```bash
AI_PROVIDER=ollama
AI_MODEL=llama2  # или llama3, mistral, codellama и т.д.
OLLAMA_URL=http://localhost:11434
```

**Плюсы:** Бесплатно, работает оффлайн, приватность
**Минусы:** Требует мощное железо (8GB+ RAM)

### 2. Anthropic Claude ⭐ (рекомендуется)

```bash
AI_PROVIDER=anthropic
AI_MODEL=claude-sonnet-4-5-20250929
ANTHROPIC_API_KEY=sk-ant-api03-...
```

**Плюсы:** Лучшее качество, быстрый, надёжный
**Минусы:** Платный ($3-15/1M tokens)

### 3. OpenAI GPT

```bash
AI_PROVIDER=openai
AI_MODEL=gpt-4
OPENAI_API_KEY=sk-...
```

**Плюсы:** Хорошее качество, популярный
**Минусы:** Платный

### 4. Google Gemini

```bash
AI_PROVIDER=google
AI_MODEL=gemini-pro
GOOGLE_API_KEY=...
```

**Плюсы:** Бесплатный tier, быстрый
**Минусы:** Ограничения на бесплатном плане

---

## 📱 Настройка SIP-клиента

После установки вы получите настройки для подключения:

### Для MicroSIP (Windows):

```
SIP Сервер:    192.168.1.X
SIP Порт:      5060
Логин:         user1
Пароль:        [показан в конце установки]
Номер:         100
Транспорт:     UDP
```

### Для Linphone (Linux):

```bash
# Установка
sudo apt install linphone

# Настройка через GUI или конфиг
```

### Для Zoiper (кросс-платформенный):

Настройки аналогичны MicroSIP.

---

## 🐛 Управление и отладка

### Основные команды

```bash
# Просмотр логов (в реальном времени)
docker compose logs -f

# Проверка статуса контейнера
docker compose ps

# Перезапуск
docker compose restart

# Остановка
docker compose down

# Запуск
docker compose up -d

# Пересборка (после изменений)
docker compose up -d --build
```

### Отладка Asterisk

```bash
# Подключение к консоли Asterisk
docker exec -it ai-callcenter asterisk -r

# Внутри консоли:
pjsip show endpoints    # Показать SIP пользователей
pjsip show contacts     # Показать подключения
core show channels      # Активные звонки
agi show commands       # AGI команды
```

### Отладка AGI скрипта

```bash
# Логи AGI
tail -f agi/logs/agi_*.log

# Или через Docker
docker compose logs -f | grep agi
```

### Тестовый звонок

Позвоните на номер **100** (эхо-тест) или **101** (AI ассистент).

---

## 🛠️ Системные требования

### Минимальные (Ollama llama2)

- **ЦП:** 4 ядра
- **RAM:** 8 GB
- **Диск:** 20 GB свободного места
- **ОС:** Ubuntu 20.04+, Debian 11+, CentOS 8+

### Рекомендуемые (Ollama llama3)

- **ЦП:** 8 ядер
- **RAM:** 16 GB
- **Диск:** 50 GB (SSD)
- **ОС:** Ubuntu 22.04+, Debian 12+

### Для облачных провайдеров (Claude/GPT)

- **ЦП:** 2 ядра
- **RAM:** 2 GB
- **Диск:** 5 GB
- Нужен интернет и API ключ

---

## 📊 Структура проекта

```
ai-callcenter-direct/
├── install.sh              # 🚀 Автоустановщик (интерактивный)
├── install_auto.sh         # 🤖 Автоустановщик (без UI)
├── setup.py                # ⚙️  Настройка проекта (вызывается из install.sh)
├── docker-compose.yml      # 🐳 Docker оркестрация
├── Dockerfile              # 🐳 Образ контейнера
├── docker-entrypoint.sh    # 🚀 Точка входа контейнера
│
├── .env                    # 🔐 Переменные окружения (генерируется)
├── config.json             # 💾 Конфигурация (генерируется)
├── CREDENTIALS.txt         # 🔑 Учётные данные (генерируется)
│
├── agi/
│   ├── agi_handler.py      # 🤖 Главный AGI скрипт
│   └── logs/               # 📊 Логи AGI
│
├── asterisk/
│   └── configs/
│       ├── pjsip.conf      # 📞 SIP конфигурация (генерируется)
│       └── extensions.conf # 📋 Диалплан (генерируется)
│
├── recordings/             # 🎙️  Записи звонков
├── requirements.txt        # 📦 Python зависимости
└── README_LINUX.md         # 📖 Эта инструкция
```

---

## 🔒 Безопасность

### Что делается автоматически:

1. **Случайные пароли** - для каждого SIP пользователя генерируется уникальный пароль
2. **Изоляция Docker** - контейнер изолирован от хост-системы
3. **Минимальные права** - AGI скрипты работают с ограниченными правами
4. **Нет открытых портов** - только SIP (5060) и RTP (10000-10100)

### Рекомендации:

- Используйте **файрвол** (ufw, iptables)
- Меняйте дефолтные порты если нужно
- Не храните API ключи в репозитории
- Регулярно обновляйте Docker образы

---

## 🎉 Готово!

**Звони и общайся с AI!** 🤖📞

---

## 📞 Поддержка

- **GitHub Issues:** [github.com/YOUR_REPO/ai-callcenter-direct/issues](https://github.com/YOUR_REPO/ai-callcenter-direct/issues)
- **Telegram:** @your_username
- **Email:** your_email@example.com

---

## 📄 Лицензия

MIT License - делайте что хотите 😎

---

## 🙏 Благодарности

- **Asterisk** - за мощный VoIP сервер
- **Ollama** - за простые локальные LLM
- **Anthropic** - за лучший AI
- **Docker** - за удобную контейнеризацию
