# 🎉 AI Call Center - Автоматическая установка готова!

## ✅ Что было создано для Linux

Ваш проект теперь полностью готов к автоматической установке на Linux-серверах!

### 📦 Созданные файлы установки:

#### 1. **install.sh** - Основной установщик (интерактивный)
```bash
sudo chmod +x install.sh
sudo ./install.sh
```

**Что делает:**
- ✅ Автоматически определяет дистрибутив Linux
- ✅ Устанавливает Docker + Docker Compose
- ✅ Устанавливает Ollama
- ✅ Загружает модель llama2 (~3.8 GB)
- ✅ Устанавливает Python зависимости
- ✅ Запускает интерактивную настройку (задаёт вопросы)
- ✅ Собирает и запускает Docker контейнер
- ✅ Показывает итоговые настройки SIP

**Время установки:** 5-10 минут

---

#### 2. **install_auto.sh** - Полностью автоматический установщик (БЕЗ вопросов)
```bash
sudo chmod +x install_auto.sh
sudo ./install_auto.sh
```

**Что делает:**
- ✅ Всё то же самое, что install.sh
- ✅ НО: без вопросов, всё берёт из переменных окружения
- ✅ Идеально для CI/CD, автоматического деплоя, серверов

**Кастомизация через переменные:**
```bash
sudo SERVER_IP=10.0.0.5 \
     AI_PROVIDER=anthropic \
     AI_MODEL=claude-sonnet-4-5-20250929 \
     ANTHROPIC_API_KEY=sk-ant-xxx \
     SIP_USER=myuser \
     SIP_PASSWORD=mypassword \
     ./install_auto.sh
```

**Все переменные** (с дефолтами):
```bash
# Базовые
PROJECT_NAME=ai-callcenter
SERVER_IP=auto-detect
SIP_PORT=5060
RTP_START=10000
RTP_END=10100

# SIP
SIP_USER=user1
SIP_PASSWORD=auto-generated
SIP_EXTENSION=100
SIP_DISPLAY_NAME=User1

# AI
AI_PROVIDER=ollama              # anthropic | openai | google | ollama
AI_MODEL=llama2
OLLAMA_URL=http://localhost:11434
AI_LANGUAGE=русский
AI_SYSTEM_PROMPT="Вы вежливый AI-ассистент..."

# API ключи
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GOOGLE_API_KEY=

# Голос
TTS_PROVIDER=openai             # google | yandex | openai
STT_PROVIDER=whisper            # google | yandex | whisper
ENABLE_RECORDING=false
```

---

#### 3. **diagnose.sh** - Диагностика системы
```bash
sudo chmod +x diagnose.sh
sudo ./diagnose.sh
```

**Что проверяет:**
- ✅ Docker установлен и запущен
- ✅ Docker Compose работает
- ✅ Ollama установлен и модели загружены
- ✅ Python + зависимости
- ✅ Конфигурация проекта (.env, config.json)
- ✅ Docker контейнер запущен
- ✅ Asterisk работает внутри контейнера
- ✅ SIP endpoints настроены
- ✅ Порты открыты
- ✅ Сеть настроена

**Пример вывода:**
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🐳 DOCKER
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[✓] Docker установлен: Docker version 24.0.7
[✓] Docker сервис запущен
[✓] Docker работает корректно

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🦙 OLLAMA
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[✓] Ollama установлен: ollama version 0.1.17
[✓] Ollama сервис запущен
[✓] Модель llama2 установлена
[✓] Модель отвечает: ок

...
```

---

#### 4. **prepare_for_linux.sh** - Подготовка файлов
```bash
chmod +x prepare_for_linux.sh
./prepare_for_linux.sh
```

**Что делает:**
- ✅ Делает все .sh файлы исполняемыми
- ✅ Конвертирует CRLF → LF (если нужно)

---

### 📚 Созданная документация:

#### 1. **README.md** - Главный README (обновлён)
- Краткое описание
- Быстрый старт для Linux и Windows
- Ссылки на полную документацию

#### 2. **README_LINUX.md** - Полная документация для Linux
- Подробное описание всех возможностей
- Инструкции по установке (3 способа)
- Архитектура системы
- Поддерживаемые AI провайдеры
- Настройка SIP-клиентов
- Управление и отладка
- Системные требования
- Безопасность
- FAQ и решение проблем

#### 3. **CHEATSHEET.md** - Шпаргалка с командами
- Быстрый старт
- Управление контейнером
- Диагностика
- Логи
- Работа с Ollama
- Сетевые команды
- Firewall
- Бэкап/восстановление
- Все переменные окружения
- Решение типичных проблем

---

## 🚀 Как использовать в Linux:

### Шаг 1: Перенесите проект на Linux сервер

```bash
# Через Git (если есть репозиторий)
git clone https://github.com/YOUR_REPO/ai-callcenter-direct.git
cd ai-callcenter-direct

# Или через scp
scp -r ai-callcenter-direct user@server:/home/user/
ssh user@server
cd ai-callcenter-direct
```

### Шаг 2: Подготовьте файлы (если переносили с Windows)

```bash
chmod +x prepare_for_linux.sh
./prepare_for_linux.sh
```

### Шаг 3: Запустите установку

**Интерактивно (с вопросами):**
```bash
sudo chmod +x install.sh
sudo ./install.sh
```

**Автоматически (без вопросов):**
```bash
sudo chmod +x install_auto.sh
sudo ./install_auto.sh
```

**С кастомными настройками:**
```bash
sudo SERVER_IP=10.0.0.5 \
     AI_PROVIDER=anthropic \
     ANTHROPIC_API_KEY=sk-ant-xxx \
     ./install_auto.sh
```

### Шаг 4: Проверьте систему

```bash
sudo ./diagnose.sh
```

### Шаг 5: Получите SIP настройки

```bash
python3 show_microsip_config.py
```

### Шаг 6: Настройте SIP-клиент и звоните!

📞 **Готово! Можно разговаривать с AI!**

---

## 📂 Полная структура созданных файлов:

```
ai-callcenter-direct/
│
├── 🚀 УСТАНОВКА (новое для Linux!)
│   ├── install.sh              ← Основной установщик
│   ├── install_auto.sh         ← Автоматический без UI
│   ├── diagnose.sh             ← Диагностика системы
│   └── prepare_for_linux.sh    ← Подготовка файлов
│
├── 📖 ДОКУМЕНТАЦИЯ (новое!)
│   ├── README.md               ← Главный README (обновлён)
│   ├── README_LINUX.md         ← Полная Linux документация
│   └── CHEATSHEET.md           ← Шпаргалка с командами
│
├── ⚙️ НАСТРОЙКА (было раньше)
│   ├── setup.py
│   ├── show_microsip_config.py
│   └── test_system.py
│
├── 🐳 DOCKER (было раньше)
│   ├── docker-compose.yml
│   ├── Dockerfile
│   └── docker-entrypoint.sh
│
├── 🤖 AGI (было раньше)
│   ├── agi/
│   │   └── agi_handler.py
│   └── requirements.txt
│
├── 📞 ASTERISK (было раньше)
│   └── asterisk/configs/
│       ├── pjsip.conf
│       └── extensions.conf
│
├── 🔧 КОНФИГУРАЦИЯ (генерируется)
│   ├── .env
│   ├── config.json
│   └── CREDENTIALS.txt
│
└── 🎙️ ДАННЫЕ
    └── recordings/
```

---

## 💡 Ключевые фишки:

### 1. **Полная автоматизация**
- Не нужно вручную устанавливать Docker, Ollama, зависимости
- Всё делается одним скриптом

### 2. **Два режима установки**
- **Интерактивный** - для первого запуска, задаёт вопросы
- **Автоматический** - для CI/CD, серверов, без вопросов

### 3. **Переменные окружения**
- Вся конфигурация через ENV
- Легко кастомизировать под любые нужды

### 4. **Диагностика**
- Полная проверка всех компонентов
- Показывает что работает, что нет

### 5. **Документация**
- README для быстрого старта
- README_LINUX для подробностей
- CHEATSHEET для команд

### 6. **Кросс-платформенность**
- Linux - основная платформа (полная автоматизация)
- Windows - для разработки (через setup.py)

---

## 🎯 Типичные сценарии использования:

### Сценарий 1: Тестирование на локальной Linux машине

```bash
git clone https://github.com/YOUR_REPO/ai-callcenter-direct.git
cd ai-callcenter-direct
sudo ./install.sh
# Ответить на вопросы интерактивно
# Через 5-10 минут всё готово!
```

### Сценарий 2: Деплой на удалённый сервер

```bash
ssh user@server
git clone https://github.com/YOUR_REPO/ai-callcenter-direct.git
cd ai-callcenter-direct
sudo AI_PROVIDER=anthropic \
     ANTHROPIC_API_KEY=$MY_KEY \
     SERVER_IP=$(hostname -I | awk '{print $1}') \
     ./install_auto.sh
# Всё устанавливается автоматически без вопросов
```

### Сценарий 3: CI/CD pipeline

```yaml
# .github/workflows/deploy.yml
- name: Deploy AI Call Center
  run: |
    ssh production << 'EOF'
      cd /opt/ai-callcenter
      git pull
      sudo SERVER_IP=$SERVER_IP \
           AI_PROVIDER=anthropic \
           ANTHROPIC_API_KEY=$ANTHROPIC_API_KEY \
           ./install_auto.sh
    EOF
```

### Сценарий 4: Множественный деплой на несколько серверов

```bash
# deploy_multi.sh
SERVERS="server1 server2 server3"
for server in $SERVERS; do
    ssh $server "cd /opt/ai-callcenter && sudo ./install_auto.sh"
done
```

---

## ✅ Что дальше:

1. **Перенесите проект на Linux сервер**
2. **Запустите `./install.sh` или `./install_auto.sh`**
3. **Проверьте через `./diagnose.sh`**
4. **Настройте SIP-клиент**
5. **Звоните и разговаривайте с AI!**

---

## 🎉 Готово!

Ваш AI Call Center теперь **полностью автоматизирован** для Linux!

**Просто запустите один скрипт - и через 10 минут у вас работающий AI колл-центр! 🤖📞**

---

**Документация:**
- [README.md](README.md) - краткое описание
- [README_LINUX.md](README_LINUX.md) - полная документация
- [CHEATSHEET.md](CHEATSHEET.md) - шпаргалка с командами

**Скрипты:**
- `install.sh` - основной установщик
- `install_auto.sh` - автоматический установщик
- `diagnose.sh` - диагностика системы
- `prepare_for_linux.sh` - подготовка файлов
