# 🚀 AI Call Center (Groq + Faster-Whisper)

**Самый простой AI колл-центр. Один скрипт = готово.**

## Установка

```bash
chmod +x install.sh
sudo ./install.sh
```

Всё. Система установлена и запущена.

## Подключение

После установки увидишь данные:

```
Сервер:  192.168.x.x:5060
Логин:   user1
Пароль:  xxxxxxxxxx
Номер:   100
```

Введи их в любой SIP клиент (Linphone, Zoiper, MicroSIP) и звони на **100**.

## Команды

```bash
# Логи
docker compose logs -f

# Перезапуск
docker compose restart

# Остановка
docker compose down

# Запуск
docker compose up -d
```

## Что внутри

- **LLM**: Groq (llama-3.1-8b-instant) - самый быстрый
- **STT**: Faster-Whisper (base) - в 5-10x быстрее обычного Whisper
- **TTS**: gTTS
- **Телефония**: Asterisk

## Настройка

Всё настраивается в `config.json` и `.env`

---

**Просто работает™**
